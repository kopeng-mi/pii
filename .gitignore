# Rust
/target
**/*.rs.bk

# Local data — DB lives at $LOCALAPPDATA/pii/pii.db (Windows)
# or ~/.local/share/pii/pii.db (Linux) — never in CWD.
/pii.db
/pii.db-shm
/pii.db-wal

# Env / secrets
.env
.env.*
!.env.example

# Editor noise
.vscode/
.idea/
*.swp
*.swo
*~

# OS noise
.DS_Store
Thumbs.db

# Test artifacts
/tmp/
